# ── CoWorkX Coordinator (backend) .env ────────────────────────────────
# Copy this file to ".env" in the backend/ folder and fill in real values.

# PostgreSQL connection string.
# NOTE: URL-encode special characters in the password (e.g. @ becomes %40).
DATABASE_URL=postgresql://postgres:YourPassword%40123@localhost:5432/coworkx
